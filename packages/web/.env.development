# Loaded by Vite when running `vite dev` (mode = "development").
# Vite proxies /api → wrangler:8787 and /auth → wrangler:8787 (vite.config.ts),
# so leave these as same-origin paths in dev.
#
# Override locally in `.env.development.local` (gitignored).
VITE_API_URL=/api
VITE_AUTH_URL=/auth
