# Loaded by Vite when running `vite build` (mode = "production").
# Values here are public — they ship inside the bundle. Do NOT put secrets.
#
# Split-domain deploy: the Worker serves the SPA on bainder.com and the API on
# api.bainder.com. Keep these aligned with packages/api/wrangler.jsonc.
VITE_API_URL=https://api.bainder.com
VITE_AUTH_URL=https://api.bainder.com/auth
